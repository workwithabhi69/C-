#include <iostream>
using namespace std;

int main() {
    int num;
    cin >> num;

    int product = 1;

    while (num > 0) {
        int digit = num % 10;
        product *= digit;
        num /= 10;
    }

    cout << "Product of digits = " << product;

    return 0;
}
