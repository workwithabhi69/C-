#include <iostream>
#include <string>
using namespace std;

int main() {
    string num;
    cin >> num;

    bool isDuck = false;

    if (num[0] == '0') {
        cout << "Not a Duck number";
        return 0;
    }

    for (int i = 1; i < num.length(); i++) {
        if (num[i] == '0') {
            isDuck = true;
            break;
        }
    }

    if (isDuck)
        cout << "Duck number";
    else
        cout << "Not a Duck number";

    return 0;
}
