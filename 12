#include <iostream>
using namespace std;

int gcd(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}

int main() {
    int n;
    cin >> n;

    int nums[n];
    for (int i = 0; i < n; i++)
        cin >> nums[i];

    int result = nums[0];
    for (int i = 1; i < n; i++)
        result = gcd(result, nums[i]);

    cout << "GCD = " << result;

    return 0;
}
